const http = require('http'); const postData = JSON.stringify({ topic: "Test workflow verification", maxCycles: 2, qualityThreshold: 0.7 }); const options = { hostname: 'localhost', port: 3000, path: '/api/workflows/start', method: 'POST', headers: { 'Content-Type': 'application/json', 'Content-Length': Buffer.byteLength(postData) } }; const req = http.request(options, (res) = console.log(`STATUS: ${res.statusCode}`); res.setEncoding('utf8'); res.on('data', (chunk) = console.log(`BODY: ${chunk}`); }); }); req.on('error', (e) = console.error(`problem with request: ${e.message}`); }); req.write(postData); req.end();  
